# Jekyll build output and caches
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# Bundler
/vendor/
/.bundle/

# macOS
.DS_Store

# Local bundle lockfile (GitHub Pages uses its own pinned toolchain)
Gemfile.lock

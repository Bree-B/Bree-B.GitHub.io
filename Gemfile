source "https://rubygems.org"

# The static site generator. GitHub Pages builds this site with its own
# pinned toolchain, so this Gemfile only needs to cover local previews.
# Jekyll 4.4 bundles webrick, so `jekyll serve` works on modern Ruby.
gem "jekyll", "~> 4.4"
